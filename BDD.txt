The program does nothing to non-alphabetical characters, since they do not contain consonants or vowels.
Example Input: 3
Example Output: 3

The program adds "way" to single-letter words beginning with a vowel.
Example Input: i
Example Output: iway

The program adds "way" to the end of multiple-letter words beggining with a vowel.
Example Input: example
Example Output: exampleway

The program takes words beggining with a single consonant and moves it to the end of the word.
Example Input: bird
Example Output: irdb

The program takes words beggining with multiple consonants and moves them to the end of the word.
Example Input: trampoline
Example Output: ampolinetr

The program adds ay to words beggining with a single consonant.
Example Input: bird
Example Output: birday

The program adds ay to words beggining with multiple consonants.
Example Input: trampoline
Example Output: trampolineay

The program takes words beggining with "qu" and moves it to the end of the word.
Example Input: quiet
Example Output: ietqu

The program takes words beggining with a consonant followed by "qu" and moves it to the end of the word.
Example Input: squeal
Example Output: ealsqu